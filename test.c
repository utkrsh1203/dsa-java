#include <stdio.h>

int main()
{
    // char name[50];
    // int marks;
    // FILE *fp;

    // fp = fopen("students.txt", "w");

    // if (fp == NULL)
    // {
    //     printf("Error opening file!\n");
    //     return 1;
    // }

    // printf("Enter student name and marks (separated by space):\n");
    // scanf("%s %d", name, &marks);

    // while (name[0] != '#')
    // {
    //     fprintf(fp, "%s %d\n", name, marks);

    //     printf("Enter student name and marks (separated by space):\n");
    //     scanf("%s %d", name, &marks);
    // }

    // fclose(fp);

    printf("Hello world");
    return 0;
}
